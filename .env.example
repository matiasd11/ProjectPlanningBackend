# Environment variables for development
NODE_ENV=development
NODE_DOCKER_PORT=5000
NODE_LOCAL_PORT=5001
API_VERSION=v1

# Database configuration (when using host network)
DB_HOST=localhost
DB_USER=projectuser
DB_PASSWORD=projectpass
DB_NAME=projectplanning
DB_PORT=3307

# MySQL Docker configuration
MYSQL_ROOT_PASSWORD=root
MYSQLDB_DATABASE=projectplanning
MYSQLDB_USER=projectuser
MYSQLDB_LOCAL_PORT=3307
MYSQLDB_DOCKER_PORT=3306

# CORS origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8080,http://localhost:4200

# Bonita BPM Configuration (Bonita Studio local)
BONITA_URL=http://host.docker.internal:8080/bonita
BONITA_USERNAME=walter.bates
BONITA_PASSWORD=bpm
BONITA_PROCESS_NAME=Proceso de Ejecución de Proyecto de Financiamiento
BONITA_LOCAL_PORT=8080

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
BCRYPT_ROUNDS=12